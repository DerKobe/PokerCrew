# Caddy automatically fetches a free HTTPS certificate (Let's Encrypt) for DOMAIN.
# HTTPS is required: browsers only allow microphone access on secure pages.
{$DOMAIN} {
	encode gzip zstd
	reverse_proxy app:3000
}
